import FollowingMatch from "../components/FollowingMatch";

it('Render the FollowingMatch Questions', () => {
    // const fetched = true;
    // const wrapper = mount(<FollowingMatch handleAns={handleAns} isAns={isAns} question={question}/>);
    // expect(wrapper.find('Result').length).toEqual(1);
})